{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "https": "nuxt dev --https --ssl-key key.pem --ssl-cert cert.pem --port 3000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.11",
    "sass": "^1.55.0"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.2",
    "nuxt-icons": "^2.0.2",
    "pinia": "^2.0.22"
  }
}
